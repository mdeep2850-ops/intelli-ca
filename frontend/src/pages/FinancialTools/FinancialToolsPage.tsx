import PlaceholderPage from '../../components/common/PlaceholderPage';
import { Calculator } from 'lucide-react';

export default function FinancialToolsPage() {
  return (
    <PlaceholderPage 
      title="Financial Tools Library"
      description="Access standalone deterministic financial calculators, tax estimators, and valuation models outside of the AI workspace."
      icon={Calculator}
    />
  );
}
