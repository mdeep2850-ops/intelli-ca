import PlaceholderPage from '../../components/common/PlaceholderPage';
import { Users } from 'lucide-react';

export default function ClientsPage() {
  return (
    <PlaceholderPage 
      title="Client Management"
      description="Manage your client profiles, associate financial documents, and track compliance status across your portfolio."
      icon={Users}
    />
  );
}
